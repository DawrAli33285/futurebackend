declare module "lune" {
  export interface MoonPhase {
    phase: number; // 0=new, 0.25=first quarter, 0.5=full, 0.75=last quarter
    illuminated: number; // 0=new, 1=full
    age: number; // days
  }
  export function phase(date: Date): MoonPhase;
}
